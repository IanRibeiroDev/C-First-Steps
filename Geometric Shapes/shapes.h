#ifndef FIRST_STEPS_IN_C_SHAPES_H
#define FIRST_STEPS_IN_C_SHAPES_H

double circumference(double radius);
double diameter(double radius);
float rectanglePerimeter(float base, float height);
float rectangleArea(float base, float height);
float squarePerimeter(float side);
float squareArea(float side);

#endif //FIRST_STEPS_IN_C_SHAPES_H
